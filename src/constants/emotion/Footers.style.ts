import { css } from "@emotion/css";
import theme from "binar/constants/index";

export const StyledFooter = css({
  borderTop: `1px solid ${theme.neutral_colors.grayscale_40}`,
  background: theme.neutral_colors.grayscale_10,
  padding: "40px 120px",
});
